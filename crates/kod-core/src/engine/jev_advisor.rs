//! Jev advisor helpers for [`super::KodEngine`].
//!
//! Placeholder module created by the engine split (S10). The `*_with_jev`
//! family currently lives in `mod.rs`; subsequent commits move each
//! method here so the entire Jev dependency surface lives in one file.
//! Being a submodule of `engine`, this file has access to `KodEngine`'s
//! private fields — no field visibility changes are required.

// Intentionally empty until the first method move.
